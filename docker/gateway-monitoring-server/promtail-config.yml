server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://100.101.178.87:3100/loki/api/v1/push # Use the ACTUAL IP here, not a variable

scrape_configs:
  - job_name: system
    static_configs:
    - targets:
        - localhost
      labels:
        job: varlogs
        host: anno-gw-mon-rpi3b-01 # Hardcode the name for now to test
        __path__: /var/log/*log
