# anno-app — local socket (Homepage runs on this host)
anno-app:
  socket: /var/run/docker.sock

# Remote hosts via Tailscale IPs — .local mDNS does not resolve inside Docker containers.
# Requires docker-socket-proxy running on each node (port 2375).
anno-ai:
  host: 100.85.193.50
  port: 2375

anno-gw:
  host: 100.101.178.87
  port: 2375

anno-nas:
  host: 100.114.225.52
  port: 2375
